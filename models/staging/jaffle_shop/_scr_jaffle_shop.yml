sources:
  - name: jaffle_shop
    database: dbt-tutorial
    schema: jaffle_shop
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 1
          period: day
      loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        config:
          freshness: null
      - name: orders

  - name: stripe
    database: dbt-tutorial
    schema: stripe
    tables:
      - name: payment

      # dbt-tutorial.stripe.payment
models/staging/jaffle_shop/_src_jaffle_shop.yml$0